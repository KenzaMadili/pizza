<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="5dp">

    <!-- Carte chocolat sombre -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@drawable/card_gourmand"
        android:elevation="4dp"
        android:minHeight="90dp">

        <!-- Image pizza -->
        <ImageView
            android:id="@+id/imgPizza"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:scaleType="centerCrop"
            android:background="@drawable/img_rounded_left_dark" />

        <!-- Texte -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center_vertical"
            android:paddingStart="14dp"
            android:paddingEnd="10dp"
            android:paddingTop="12dp"
            android:paddingBottom="12dp">

            <TextView
                android:id="@+id/tvNom"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textStyle="bold"
                android:textSize="15sp"
                android:textColor="#D4A843"
                android:fontFamily="sans-serif-medium"
                android:maxLines="1"
                android:ellipsize="end" />

            <!-- Accent doré -->
            <View
                android:layout_width="28dp"
                android:layout_height="1.5dp"
                android:background="#D4A843"
                android:alpha="0.6"
                android:layout_marginTop="5dp"
                android:layout_marginBottom="6dp" />

            <TextView
                android:id="@+id/tvMeta"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:textColor="#8B5E6B"
                android:fontFamily="sans-serif-light"
                android:maxLines="2"
                android:ellipsize="end" />

        </LinearLayout>

        <!-- Chevron doré -->
        <ImageView
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_gravity="center_vertical"
            android:layout_marginEnd="12dp"
            android:src="@drawable/ic_chevron_right_gold"
            android:alpha="0.7" />

    </LinearLayout>

</RelativeLayout>
